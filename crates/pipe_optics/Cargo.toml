[package]
name = "pipe_optics"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Deterministic, physically interpretable structured-light sensing simulation for the Pipe"
readme = "README.md"

[features]
default = []

[dependencies]
